# ================================================================
# requirements.txt  —  зависимости для бота-администратора
# Версия: 3.3.1, совместима с Windows и Linux (без звука)
# ================================================================

python-telegram-bot>=22.0
psutil>=5.9.0
Pillow>=10.0.0
pyautogui>=0.9.54
comtypes>=1.4.0          # требуется только для Windows (управление звуком)
pyinstaller>=6.0.0       # для сборки EXE (только на Windows)

# Дополнительно: если вы используете Linux и не планируете управлять звуком,
# comtypes можно закомментировать. Для pyautogui на Linux могут потребоваться
# системные пакеты: python3-xlib, libxcb, scrot (для скриншотов).
# В Windows эти библиотеки не нужны.

# Если вы хотите использовать запись/воспроизведение звука, добавьте:
# pyaudio>=0.2.11   (требует portaudio19-dev на Linux, на Windows — отдельный установщик)
